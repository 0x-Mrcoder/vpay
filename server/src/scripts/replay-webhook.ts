
/**
 * Replay Webhook
 * 
 * NOTE: This script is temporarily disabled as it depends on Zainpay types which have been removed.
 * TODO: Update to support new webhook events if needed.
 */

const replayWebhook = async () => {
    console.log('Replaying webhook...');
    console.log('⚠️  This script is disabled because Zainpay types are no longer available.');
    process.exit(0);
};

replayWebhook();
